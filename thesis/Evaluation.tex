\chapter{Evaluación experimental}\label{chapter:evaluation}

Este capítulo presenta la evaluación experimental de los dos módulos centrales que el Capítulo~\ref{chapter:proposal} introdujo como aportación original de BT-GRAPHRAG: la resolución contextual y temporal de entidades (CGER) y la detección y resolución de conflictos temporales (ETCDR). Cada módulo se evalúa de forma independiente sobre un corpus específicamente preparado para él, con ganancia de verdad anotada manualmente y métricas pareadas a su tipo de salida. ETCDR se evalúa de manera conjunta con la clasificación de cardinalidad, ya que su lógica de detección depende por completo de la categoría asignada a cada tipo de relación y, por tanto, ninguna evaluación significativa puede aislar una de la otra.

El capítulo se organiza en siete secciones. La sección~\ref{sec:eval-setup} expone la metodología común a los dos experimentos: la naturaleza del corpus, los modelos empleados, el proceso de generación de la verdad anotada y las métricas reportadas. La sección~\ref{sec:eval-cger} presenta la evaluación de CGER, incluyendo el ajuste de su umbral coseno mediante búsqueda por recocido simulado y los resultados pareados sobre el conjunto de prueba. La sección~\ref{sec:eval-etcdr} describe la evaluación conjunta de ETCDR y la clasificación de cardinalidad, con la matriz de confusión y el análisis de errores. La sección~\ref{sec:eval-ragtest} complementa los dos experimentos anteriores con un análisis observacional sobre un corpus de cien documentos, ejecutado sin verdad anotada, que permite caracterizar el comportamiento de ambos módulos a una escala mayor que la de los conjuntos controlados. La sección~\ref{sec:eval-timeqa} reporta una evaluación \emph{end-to-end} sobre el partido \texttt{hard} de TimeQA, que compara el motor de consulta temporal de BT-GRAPHRAG con la línea base de GraphRAG sobre 370~preguntas con anclaje temporal explícito. La sección~\ref{sec:eval-discussion} discute los resultados, sus limitaciones y las lecturas que pueden extraerse de cada experimento. Finalmente, la sección~\ref{sec:eval-conclusions} resume las conclusiones del capítulo.

%% ============================================================
\section{Metodología común}\label{sec:eval-setup}

La evaluación se realiza sobre un corpus controlado de cinco artículos biográficos extraídos de Wikipedia\cite{TODO_wikipedia}, seleccionados por presentar las dos propiedades necesarias para ejercitar los dos módulos: una densidad alta de menciones con variantes nominales (necesaria para CGER) y un conjunto de hechos con restricciones temporales claras y, en algunos casos, evolución de cargos a lo largo del tiempo (necesario para ETCDR). El conjunto de prueba lo integran las biografías de Agnes de Selincourt, Angela Eagle, Charles Fechter, Peter Sutherland y Thomas Slingsby Duncombe, con un volumen combinado de aproximadamente 8\,700 palabras. Estos perfiles cubren un rango deliberadamente amplio de épocas (siglo XIX al XXI) y dominios (política, academia, artes escénicas y misiones religiosas), lo que reduce el riesgo de que los resultados respondan a un sesgo de género temporal o de campo.

Para la calibración del umbral coseno de CGER (sección~\ref{sec:eval-cger}) se reserva un corpus de entrenamiento independiente compuesto por quince biografías distintas (Frederica Wilson, Robert Tarjan, Margaret Burbidge, Wilhelm Liebknecht, Tina Anselmi, Philip Alston y otros), con un volumen total de aproximadamente 17\,500 palabras. La separación estricta entre los corpus de entrenamiento y de prueba garantiza que los umbrales se elijan sin observar las decisiones del conjunto sobre el que se reporta el resultado final.

\paragraph{Pipeline de extracción.} En ambos corpus, cada documento se segmenta en bloques de \emph{tokens} (\emph{chunks}) mediante el segmentador estándar de GraphRAG, con tamaño de bloque de 800~\emph{tokens} y solapamiento de 100 para el conjunto de prueba de CGER, y de 600~\emph{tokens} con el mismo solapamiento para el de ETCDR (la ventana más estrecha favorece la detección de hechos temporales por proximidad). Sobre cada bloque se ejecuta el extractor temporal descrito en la sección~\ref{sec:impl-stage1}, configurado con \texttt{gpt-4.1-mini}\cite{TODO_gpt41mini} como modelo de generación y \texttt{text-embedding-3-large}\cite{TODO_te3large} (de 3\,072 dimensiones) como modelo de \emph{embedding}. La salida concatenada de la extracción se materializa como un trío de archivos JSON ---bloques, entidades y relaciones--- que sirve de entrada común para todos los experimentos posteriores: ningún experimento vuelve a invocar al extractor, lo que aísla las métricas reportadas de la variabilidad propia del modelo de extracción.

\paragraph{Verdad anotada.} La verdad de cada experimento se genera en dos pasos. En el primero, un agente LLM al que se le entrega exclusivamente el archivo de extracción agrupa las menciones que considera referidas a la misma entidad o predicado, siguiendo un \emph{prompt} de autoría que prohibe la introducción de cadenas que no aparezcan textualmente en la extracción y que descarta los grupos de un solo elemento. En el segundo, los grupos propuestos por el agente se revisan manualmente para corregir falsos agrupamientos, completar los que falten y descartar los que el lector humano considere ambiguos. Esta política conservadora prefiere descartar un \emph{cluster} dudoso antes que incluirlo, ya que el corrector pareado (sección~\ref{sec:eval-setup-metrics}) penaliza más una asignación errónea que una omitida.

\paragraph{Métricas reportadas.}\label{sec:eval-setup-metrics}
Para CGER, cuya salida natural es un mapa de fusión entre menciones, el corrector convierte cada \emph{cluster} de la verdad anotada en el conjunto de pares no ordenados $\{(a_i, a_j)\,|\,a_i, a_j \in \text{cluster}, i<j\}$, hace lo mismo con el mapa de fusión emitido por el módulo y reporta precisión, exhaustividad y $F_1$ pareadas. La métrica pareada es estándar en la literatura de resolución de entidades~\cite{TODO_pairwise_er} y tiene la propiedad deseable de penalizar de forma simétrica las fusiones excesivas y las omitidas, sin depender del orden de los elementos dentro de cada \emph{cluster}.

Para ETCDR, cuya salida es una estrategia de resolución por arista candidata, el corrector pairea cada entrada de la verdad anotada con la $k$-ésima aparición de su tripleta canónica en el lote y compara la estrategia esperada con la efectivamente seleccionada. Las métricas reportadas son: la precisión global (\emph{accuracy}), entendida como la proporción de tripletas correctamente clasificadas sobre el total de las pareadas; la matriz de confusión a nivel de estrategia; y el desglose de aquellas tripletas declaradas en la verdad que el sistema no llegó a procesar (\emph{unmatched}), ya sea porque la canonicalización las descartó o porque el orden de extracción difería de la previsión del anotador.

%% ============================================================
\section{Evaluación de CGER}\label{sec:eval-cger}

\subsection{Banco de pruebas y procedimiento}

Sobre el conjunto de prueba descrito en la sección anterior, el extractor produjo 690 entidades y 764 relaciones. La verdad anotada contiene 29 \emph{clusters} de entidades coreferentes que, al expandirse a pares, suman 33 pares positivos. Cada par representa dos cadenas distintas que se refieren a la misma entidad del mundo real (por ejemplo, ``BRIAN LENIHAN'' y ``BRAIN LENIHAN'' como variantes ortográficas de la misma persona, o ``2016 UNITED KINGDOM EUROPEAN UNION MEMBERSHIP REFERENDUM'' y ``BREXIT REFERENDUM 2016'' como denominaciones equivalentes del mismo evento). De forma deliberada, ninguno de los 33 pares positivos comparte la cadena textual exacta de ambos lados: la coincidencia de nombre del GraphRAG original obtendría una exhaustividad de cero sobre este conjunto, lo que permite atribuir cualquier acierto del módulo a su componente semántico-temporal y no a un \emph{baseline} trivial.

El módulo se invoca en modo \emph{intra-batch}: el grafo persistente se mantiene vacío para forzar a que cada alias se contraste contra los demás alias del mismo lote, replicando la condición del primer procesamiento de un corpus. La configuración fija el coseno-umbral en el valor obtenido en la calibración (descrita más abajo), el modelo de \emph{embedding} en \texttt{text-embedding-3-large}, el modelo de verificación en \texttt{gpt-4.1-mini} y los parámetros \texttt{cger\_candidate\_top\_k} y \texttt{cger\_phase\_b\_top\_k} en 3. Cada experimento se ejecuta tres veces, con semilla distinta del LLM, y los resultados se reportan como media y desviación estándar.

\subsection{Calibración del umbral coseno}

El umbral $\tau_{\cos}$ que controla la transición entre el pre-filtrado y la verificación por LLM admite, para un mismo corpus, configuraciones muy distintas según se priorice precisión, exhaustividad o coste en llamadas al LLM. La calibración se realiza sobre el corpus de entrenamiento independiente (118 pares positivos en la verdad anotada) mediante una búsqueda por recocido simulado~\cite{TODO_simulated_annealing} que optimiza una puntuación compuesta de $F_1$ y de proporción de llamadas al LLM:
\begin{equation}
\text{score}(\tau) = F_1(\tau) - \lambda \cdot \text{llm\_rate}(\tau),
\end{equation}
con $\lambda = 0.3$. Esta función premia la calidad de las fusiones y penaliza linealmente la fracción de pares que activan la verificación por LLM, evitando los óptimos triviales que clavarían el umbral en el extremo bajo del rango (todo se verifica) o en el alto (nada se verifica).

La búsqueda explora el intervalo $[0.20, 0.95]$ con cinco puntos iniciales de \emph{grid scan} seguidos de veinte iteraciones de recocido (temperatura inicial $0.2$, factor de enfriamiento geométrico $0.85$, radio de vecindad decreciente entre $0.25$ y $0.01$). El mejor punto encontrado fue $\tau_{\cos} = 0.687$, con $F_1 = 0.734$, precisión $0.757$ y exhaustividad $0.712$ sobre el corpus de entrenamiento, activando la verificación por LLM en un 39\% de los pares candidatos.

\subsection{Resultados sobre el conjunto de prueba}

Con el umbral fijado, las tres corridas independientes sobre el conjunto de prueba arrojan los resultados de la Tabla~\ref{tab:eval-cger-results}.

\begin{table}[h]
\centering
\small
\begin{tabular}{lcccc}
\hline
\textbf{Configuración} & \textbf{Precisión} & \textbf{Exhaustividad} & \textbf{$F_1$} & \textbf{Pares correctos / totales} \\ \hline
CGER (media de 3 corridas) & $0.600 \pm 0.010$ & $0.606 \pm 0.000$ & $0.603 \pm 0.005$ & $20 / 33$ \\
\emph{Baseline} coincidencia exacta de nombre & $0.000$ & $0.000$ & $0.000$ & $0 / 33$ \\ \hline
\end{tabular}
\caption{Resultados de CGER sobre el conjunto de prueba (33 pares positivos en la verdad anotada). El \emph{baseline} es el comportamiento de GraphRAG original, que sólo fusiona cadenas idénticas.}
\label{tab:eval-cger-results}
\end{table}

La precisión y la exhaustividad se sitúan en torno al 0.60, lo que indica que el módulo identifica correctamente alrededor de dos terceras partes de las equivalencias del conjunto de prueba sin acumular sobre-fusiones desproporcionadas. La estabilidad entre corridas es alta (desviación estándar de $F_1$ por debajo de 0.01), lo que sugiere que el componente principal de variabilidad ---el muestreo del LLM--- no introduce ruido relevante para la decisión de fusión a este umbral.

El contraste frente al \emph{baseline} de coincidencia exacta es decisivo: ninguno de los 33 pares positivos se detectaría por igualdad textual, porque todos involucran variantes ortográficas, abreviaturas, expansiones o reordenamientos. La $F_1$ de $0.603$ es, por construcción, la contribución neta del componente CGER respecto a la línea base de GraphRAG.

\subsection{Análisis de errores}

Los 13 falsos negativos comunes a las tres corridas se concentran en pares con coseno por debajo del umbral $\tau_{\cos}=0.687$: descripciones muy escuetas (``EAGLE'' frente a ``ANGELA EAGLE'') o casos donde la entidad aparece una sola vez en el corpus, lo que limita el contenido del \emph{embedding} de descripción. Estos errores son inherentes al diseño en dos fases: bajar el umbral los recuperaría a costa de duplicar el número de llamadas al LLM, según se observa durante la calibración del corpus de entrenamiento.

Los 13--14 falsos positivos típicos corresponden a entidades semánticamente vecinas pero distintas: pares como ``EUROPEAN PERSON OF THE YEAR AWARD'' y ``THE GOLD MEDAL OF THE EUROPEAN PARLIAMENT'' tienen descripciones lo bastante cercanas para que el LLM emita ocasionalmente \texttt{SAME} en lugar de \texttt{DIFFERENT\_ENTITY}. Estos errores son de la clase que la limitación~5 del estado del arte (sección~\ref{sec:soa-conclusions}) ya señala como difícil para cualquier resolución basada exclusivamente en similitud semántica; el veredicto \texttt{DIFFERENT\_TEMPORAL} reduce ---pero no elimina--- esta confusión cuando los períodos activos de las dos entidades coinciden en la misma franja temporal.

%% ============================================================
\section{Evaluación de ETCDR y clasificación de cardinalidad}\label{sec:eval-etcdr}

\subsection{Acoplamiento de los dos componentes}

ETCDR no puede evaluarse de manera aislada respecto a la clasificación de cardinalidad: la categoría asignada a cada tipo de relación determina qué consultas de detección se lanzan sobre el grafo y, por tanto, qué aristas conflictivas alcanzan al \emph{Decision Router}. Si la clasificación etiqueta una relación exclusiva como \texttt{NON\_EXCLUSIVE}, ningún conflicto se detecta y la estrategia degenera a \texttt{NEW\_EDGE}, con independencia de lo que el \emph{Decision Router} hubiera decidido en presencia de evidencia. A la inversa, una etiqueta exclusiva sobre una relación que no lo es genera conflictos espurios que el \emph{Decision Router} ha de descartar uno a uno. Por este motivo, el experimento que sigue evalúa al sistema combinado ---clasificador de cardinalidad más \emph{Decision Router}--- frente a una verdad anotada que codifica directamente la estrategia esperada sobre cada arista, sin separar la responsabilidad entre los dos sub-componentes.

\subsection{Banco de pruebas y procedimiento}

El experimento opera sobre el mismo conjunto de cinco biografías que el de CGER. Una vez completada la extracción, los 690 entidades y 764 relaciones se canonicalizan apoyándose en los \emph{clusters} de la verdad anotada de entidades y de tipos de relación: el equivalente al mapa de fusión que produciría CGER en condiciones ideales, suministrado aquí como oráculo para que la evaluación se centre en las decisiones de ETCDR y no en la calidad de la fase previa.

Para garantizar que las decisiones de detección y resolución sean comparables entre experimentos, se mantiene la base de datos persistida vacía durante toda la pasada: todos los conflictos se materializan en el ámbito intra-lote, lo que efectivamente prueba la lógica de Phase~B de ETCDR descrita en la sección~\ref{sec:bt-etcdr} y su almacén auxiliar \texttt{etcdrbatch}. Este enfoque es deliberado: la lógica Cypher que aplica \texttt{apply\_evolution}, \texttt{apply\_correction} y \texttt{apply\_corroboration} es la misma en ambos orígenes (persistido o intra-lote), de modo que la estrategia seleccionada para cada candidata es lo que efectivamente se evalúa, con independencia del subsistema que aportó la arista existente.

\paragraph{Verdad anotada.} La verdad anotada se genera siguiendo el mismo procedimiento que el de CGER: un agente LLM construye un borrador con la estrategia esperada por cada aparición de cada tripleta canónica y un revisor humano corrige y completa. Una entrada típica codifica, para una tripleta canónica como (\texttt{ELON MUSK}, \texttt{IS\_CEO\_OF}, \texttt{TESLA}), una secuencia ordenada de estrategias ---\texttt{NEW\_EDGE} en la primera ocurrencia, \texttt{CORROBORATION} en cada reiteración del mismo hecho, \texttt{EVOLUTION} si más tarde aparece un sucesor en el mismo cargo--- que el corrector pareará con la $k$-ésima aparición de esa tripleta en el orden de extracción. El conjunto resultante contiene 277 entradas: 130 con estrategia esperada \texttt{NEW\_EDGE}, 140 con \texttt{CORROBORATION} y 7 con \texttt{EVOLUTION}. La ausencia de \texttt{CORRECTION} y \texttt{DISAGREEMENT} en la verdad anotada refleja la naturaleza del corpus: los hechos están razonablemente bien acotados y no se observa ninguna retractación explícita entre documentos.

\paragraph{Sobreescrituras de cardinalidad.} La verdad anotada fija manualmente la cardinalidad de los ocho tipos de relación que el revisor identifica como claramente exclusivos en el dominio: \texttt{IS\_PRESIDENT\_OF}, \texttt{IS\_CHAIRMAN\_OF}, \texttt{IS\_DIRECTOR\_OF}, \texttt{IS\_DEPUTY\_LEADER\_OF}, \texttt{DIED\_IN}, \texttt{INTERRED\_AT}, \texttt{RETIRED\_TO} y \texttt{BORN\_IN}. Los otros 145 tipos de relación que aparecen en el lote se clasifican \emph{en línea} por el LLM, con el mismo \emph{prompt} que la sección~\ref{sec:impl-cardinality} expone y bajo la misma política conservadora que favorece \texttt{NON\_EXCLUSIVE} en ausencia de evidencia. El reparto resultante de las 153 cardinalidades efectivamente usadas se recoge en la Tabla~\ref{tab:eval-cardinality-distribution}.

\begin{table}[h]
\centering
\small
\begin{tabular}{lc}
\hline
\textbf{Categoría} & \textbf{Tipos clasificados} \\ \hline
\texttt{NON\_EXCLUSIVE} & 123 \\
\texttt{SUBJECT\_EXCLUSIVE} & 24 \\
\texttt{OBJECT\_EXCLUSIVE} & 5 \\
\texttt{BOTH\_EXCLUSIVE} & 1 \\ \hline
\textbf{Total} & 153 \\ \hline
\end{tabular}
\caption{Distribución de las cardinalidades efectivamente usadas en la corrida (8 sobreescrituras manuales y 145 clasificaciones por LLM).}
\label{tab:eval-cardinality-distribution}
\end{table}

El predominio de \texttt{NON\_EXCLUSIVE} (123 de 153) confirma que la política conservadora del clasificador se respeta también en este corpus: sólo el 20\% de los tipos de relación recibe una etiqueta exclusiva, lo que mantiene acotado el número de candidatos que invocan al \emph{Decision Router}.

\subsection{Resultados}

La Tabla~\ref{tab:eval-etcdr-scoring} recoge la métrica global y la Tabla~\ref{tab:eval-etcdr-confusion} la matriz de confusión por estrategia. La precisión global es de $0.981$ (257 aciertos en 262 entradas pareadas); de las 277 entradas de la verdad anotada, 15 no encuentran contraparte en el lote canonicalizado y se reportan como \emph{unmatched}.

\begin{table}[h]
\centering
\small
\begin{tabular}{lc}
\hline
\textbf{Indicador} & \textbf{Valor} \\ \hline
Entradas esperadas (verdad anotada) & 277 \\
Entradas pareadas con el lote canonicalizado & 262 \\
Entradas no pareadas (\emph{unmatched}) & 15 \\
Decisiones correctas & 257 \\
\textbf{Precisión global} (\emph{accuracy}) & $\mathbf{0.981}$ \\
Umbral de confianza del \emph{Decision Router} & $0.7$ \\ \hline
\end{tabular}
\caption{Resultados globales del experimento conjunto de cardinalidad y ETCDR.}
\label{tab:eval-etcdr-scoring}
\end{table}

\begin{table}[h]
\centering
\small
\begin{tabular}{lccccc}
\hline
& \multicolumn{5}{c}{\textbf{Estrategia predicha}} \\
\cline{2-6}
\textbf{Estrategia esperada} & \texttt{NEW\_EDGE} & \texttt{CORROBORATION} & \texttt{EVOLUTION} & \texttt{CORRECTION} & \texttt{DISAGREEMENT} \\ \hline
\texttt{NEW\_EDGE} (128) & \textbf{126} & 1 & 1 & 0 & 0 \\
\texttt{CORROBORATION} (127) & 0 & \textbf{124} & 3 & 0 & 0 \\
\texttt{EVOLUTION} (7) & 0 & 0 & \textbf{7} & 0 & 0 \\
\texttt{CORRECTION} (0) & --- & --- & --- & --- & --- \\
\texttt{DISAGREEMENT} (0) & --- & --- & --- & --- & --- \\ \hline
\end{tabular}
\caption{Matriz de confusión a nivel de estrategia sobre las 262 entradas pareadas. La columna sin etiqueta absorbe los ceros.}
\label{tab:eval-etcdr-confusion}
\end{table}

Los siete casos esperados como \texttt{EVOLUTION} se recuperan en su totalidad. Las cinco decisiones incorrectas se reparten entre dos confusiones distintas: tres entradas etiquetadas como \texttt{CORROBORATION} en la verdad anotada se resuelven como \texttt{EVOLUTION} porque el \emph{Decision Router} interpreta una pequeña discrepancia temporal entre las dos descripciones como un cambio sucesivo; y dos entradas \texttt{NEW\_EDGE} se reasignan a \texttt{CORROBORATION} y \texttt{EVOLUTION} cuando la canonicalización conecta dos aristas que el revisor humano consideraba independientes. Ninguna entrada termina en \texttt{CORRECTION} ni en \texttt{DISAGREEMENT}, en consonancia con la composición del corpus, que no contiene retractaciones explícitas.

De las 764 aristas candidatas que se llevaron al \emph{pipeline} completo, 208 activaron al menos una consulta de detección con resultado positivo (es decir, se enfrentaron a un conflicto intra-lote y pasaron por el \emph{Decision Router}). La distribución completa de estrategias emitidas por el módulo a lo largo de las 764 aristas se reparte de la siguiente forma: 599 \texttt{NEW\_EDGE}, 143 \texttt{CORROBORATION}, 16 \texttt{EVOLUTION} y 6 \texttt{DISAGREEMENT}. Las seis decisiones de \texttt{DISAGREEMENT} corresponden a casos en los que el valor de confianza emitido por el LLM cae por debajo del umbral de $0.7$ configurado para esta corrida, lo que activa la política de degradación descrita en la sección~\ref{sec:bt-etcdr}: en ausencia de respaldo suficiente, el sistema prefiere preservar ambas versiones marcadas como \texttt{disputed} antes que tomar una decisión irreversible.

\subsection{Análisis de errores y de entradas no pareadas}

Las 15 entradas \emph{unmatched} de la Tabla~\ref{tab:eval-etcdr-scoring} se explican por dos fenómenos. En el primero, la tripleta esperada se refiere a una arista que la canonicalización dejó fuera del lote (porque el alias correspondiente no entró en el \emph{cluster} de la verdad anotada); el corrector las reporta como informativas y no las puntúa, en consonancia con el principio conservador discutido en la sección~\ref{sec:eval-setup}. En el segundo, la $k$-ésima ocurrencia esperada no llega a aparecer en el orden de extracción, situación que ocurre cuando dos documentos describen el mismo hecho con descripciones algo distintas y el extractor no produce la segunda copia.

Los cinco errores ---tres \texttt{CORROBORATION}~$\to$~\texttt{EVOLUTION} y dos \texttt{NEW\_EDGE}~$\to$~estrategia distinta--- corresponden a la frontera natural entre \emph{el mismo hecho descrito dos veces} y \emph{dos hechos consecutivos que describen estados ligeramente distintos}. El \emph{Decision Router} dispone, para esta distinción, de las dos descripciones textuales y la cuádrupla temporal de las aristas implicadas. Cuando los intervalos $[t_v^s, t_v^e]$ están desplazados de forma marginal ---por ejemplo, porque un documento data el inicio del cargo a partir del año y otro a partir del mes--- el modelo tiende a interpretar la diferencia como una transición sucesoria, no como una redundancia. Esta sensibilidad es deliberada: aceptarla produce, en este corpus, una tasa de error del 1.9\%; eliminarla exigiría una desensibilización temporal que comprometería la detección de las verdaderas transiciones (de las que el corpus contiene siete y el módulo las recupera todas).

%% ============================================================
\section{Análisis observacional sobre un corpus extenso}\label{sec:eval-ragtest}

Los dos experimentos anteriores miden a CGER y ETCDR contra una verdad anotada manualmente, lo que requiere por construcción un corpus reducido. Para complementar esa visión, esta sección reporta una traza completa de la indexación de un corpus de cien documentos heterogéneos ---biografías políticas y deportivas, artículos de organizaciones internacionales, eventos electorales e instituciones académicas--- procesados con la misma configuración que el resto del capítulo (\texttt{gpt-4.1-mini} como modelo de generación, \texttt{text-embedding-3-large} de 3\,072~dimensiones, $\tau_{\cos} = 0.687$, umbral de confianza del \emph{Decision Router} en $0.7$). El corpus produjo 7\,969 entidades canónicas y 9\,609 relaciones tras el \emph{pipeline} completo. No existe verdad anotada para este corpus, de modo que las cifras que siguen no son métricas de calidad sino \emph{estadísticas operacionales} de los dos módulos: qué proporción de candidatos cada uno resolvió, en qué clase decidió y cuál es la distribución de los conflictos detectados.

\subsection{Trazas de CGER}

La indexación de este corpus se ejecutó sobre un grafo persistido vacío y se completó en una única pasada. Bajo esas condiciones la Fase~A del módulo ---el contraste contra el grafo persistido--- no llega a hacer ningún trabajo de resolución efectivo: el \emph{log} contiene 9\,007 entradas etiquetadas como \texttt{A\_cross\_graph}, pero todas ellas tienen \texttt{best\_match} vacío, \texttt{best\_score~=~0} y lista de comparaciones vacía, porque el bucle iteró sobre cada entidad sin ningún candidato persistido contra el que medirse. Toda la actividad de resolución de CGER se concentra, por tanto, en la Fase~B (intra-lote), cuya distribución se recoge en la Tabla~\ref{tab:eval-ragtest-cger-decisions}.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrr}
\hline
\textbf{Decisión (Fase~B)} & \textbf{Entradas} & \textbf{Proporción} \\ \hline
\texttt{BELOW\_THRESHOLD} (no candidato sobre $\tau_{\cos}$) & 4\,625 & $65.9\%$ \\
\texttt{LLM\_DIFFERENT\_ENTITY} (LLM rechaza el par)   & 1\,883 & $26.8\%$ \\
\texttt{SAME\_B} (LLM fusiona, sobrevive el nombre B)  &   350 &  $5.0\%$ \\
\texttt{SAME\_A} (LLM fusiona, sobrevive el nombre A)  &   154 &  $2.2\%$ \\
\texttt{LLM\_DIFFERENT\_TEMPORAL} (mismo referente, distinta época) & 4 & $0.06\%$ \\ \hline
\textbf{Total Fase~B} & \textbf{7\,016} & \textbf{$100\%$} \\ \hline
\end{tabular}
\caption{Distribución de decisiones de CGER en la Fase~B (intra-lote) sobre el corpus de cien documentos.}
\label{tab:eval-ragtest-cger-decisions}
\end{table}

La tasa efectiva de verificación por LLM ---la proporción de candidatos de la Fase~B que superan $\tau_{\cos}$ y se envían al verificador--- es del $34.1\%$ ($2\,391$ pares verificados sobre $7\,016$ candidatos). De los pares verificados, el LLM emite un veredicto de fusión (\texttt{SAME\_A} o \texttt{SAME\_B}) en un $21.1\%$ de los casos. El mapa de fusión final agrega 502~entradas, lo que indica una reducción nominal de aproximadamente el $6\%$ del catálogo de entidades extraídas (502~alias absorbidos sobre 8\,471 títulos antes de la fusión). Sólo una cadena de fusión transitiva (\emph{src}~$\to$~\emph{mid}~$\to$~\emph{dst}) aparece en la traza, lo que confirma empíricamente la estabilidad del orden de procesamiento intra-lote frente a las reasignaciones encadenadas que la sección~\ref{sec:bt-cger} señala como riesgo teórico.

La Tabla~\ref{tab:eval-ragtest-cger-samples} reúne ejemplos representativos del comportamiento del módulo en cada uno de los tres tipos de decisión cualitativamente más interesantes.

\begin{table}[h]
\centering
\small
\begin{tabular}{p{4.2cm}p{4.2cm}p{1.0cm}p{4.0cm}}
\hline
\textbf{Entidad nueva} & \textbf{Mejor candidato} & \textbf{$\cos$} & \textbf{Veredicto} \\ \hline
\texttt{RCAF} & \texttt{ROYAL CANADIAN AIR FORCE} & --- & \texttt{SAME\_B} (acrónimo) \\
\texttt{HITLER} & \texttt{ADOLF HITLER} & $0.79$ & \texttt{SAME\_B} (apellido $\to$ nombre canónico) \\
\texttt{ANDRE MALRAUX} & \texttt{ANDRÉ MALRAUX} & --- & \texttt{SAME\_B} (acento) \\
\texttt{INT.\ AMATEUR ATHLETIC FED.} & \texttt{WORLD ATHLETICS} & --- & \texttt{SAME\_B} (cambio de nombre) \\
\texttt{LOS ANGELES COUNTY SHERIFFS DEPT.} & \texttt{LOS ANGELES COUNTY SHERIFF'S DEPT.} & --- & \texttt{SAME\_B} (apóstrofo) \\
\hline
\texttt{WEMBLEY STADIUM (NEW)} & \texttt{WEMBLEY STADIUM (OLD)} & $0.81$ & \texttt{LLM\_DIFFERENT\_ENTITY} (cualificador) \\
\texttt{OISE RIVER} & \texttt{SEINE RIVER} & $0.77$ & \texttt{LLM\_DIFFERENT\_ENTITY} (otro referente) \\
\texttt{LIBERAL ALLIANCE OF MONTENEGRO} & \texttt{LIBERAL PARTY OF MONTENEGRO} & $0.78$ & \texttt{LLM\_DIFFERENT\_ENTITY} (organizaciones distintas) \\
\hline
\texttt{VALPARAISO MALE AND FEMALE COLLEGE} & \texttt{VALPARAISO UNIVERSITY} & $0.69$ & \texttt{LLM\_DIFFERENT\_TEMPORAL} (misma institución, dos estados) \\
\texttt{ELECTORATE OF HANOVER} & \texttt{PRINCIPALITY OF CALENBERG} & $0.72$ & \texttt{LLM\_DIFFERENT\_TEMPORAL} (sucesión territorial) \\ \hline
\end{tabular}
\caption{Ejemplos cualitativos de cada uno de los tres tipos de decisión del verificador LLM de CGER sobre el corpus de cien documentos.}
\label{tab:eval-ragtest-cger-samples}
\end{table}

Tres observaciones cabe destacar. La primera es que la mayoría de los aciertos del módulo se corresponden con clases canónicas de la literatura de \emph{entity resolution}: acrónimos, expansiones, normalización de acentos y puntuación, y renombrados institucionales (\emph{Aurore}, ahora \emph{World Athletics}). La segunda es que el filtro coseno por sí solo no separa correctamente los pares positivos de los negativos: las parejas \texttt{WEMBLEY (NEW)}/\texttt{WEMBLEY (OLD)} y \texttt{OISE RIVER}/\texttt{SEINE RIVER} exceden el umbral configurado y serían fusiones espurias en un sistema sin verificación contextual. La tercera es que el veredicto \texttt{DIFFERENT\_TEMPORAL} se invoca con extrema parsimonia (4~veces en todo el corpus): el módulo lo reserva para casos en los que dos descripciones describen efectivamente estados sucesivos del mismo referente, no para preservar identidades distintas que comparten un fragmento de nombre. Estas trazas, por su volumen, refuerzan las observaciones que el experimento controlado de la sección~\ref{sec:eval-cger} hace sobre un conjunto pequeño y reducen la sospecha de que aquellos resultados respondan a singularidades del corpus de prueba.

\subsection{Trazas de ETCDR}

Sobre el mismo corpus, ETCDR procesa 10\,276 aristas candidatas. La Tabla~\ref{tab:eval-ragtest-etcdr-strategies} recoge la distribución de las cinco estrategias emitidas por el \emph{Decision Router}, y la Tabla~\ref{tab:eval-ragtest-etcdr-cardinality} la distribución de cardinalidades efectivamente asignadas por el clasificador a los tipos de relación.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrr}
\hline
\textbf{Estrategia} & \textbf{Aristas} & \textbf{Proporción} \\ \hline
\texttt{NEW\_EDGE}      & 9\,966 & $97.0\%$ \\
\texttt{CORROBORATION}  &   284  & $2.8\%$ \\
\texttt{EVOLUTION}      &    18  & $0.18\%$ \\
\texttt{CORRECTION}     &     4  & $0.04\%$ \\
\texttt{DISAGREEMENT}   &     4  & $0.04\%$ \\ \hline
\textbf{Total} & \textbf{10\,276} & \textbf{$100\%$} \\ \hline
\end{tabular}
\caption{Distribución de estrategias emitidas por ETCDR sobre las 10\,276 aristas candidatas del corpus de cien documentos.}
\label{tab:eval-ragtest-etcdr-strategies}
\end{table}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrr}
\hline
\textbf{Cardinalidad} & \textbf{Aristas} & \textbf{Proporción} \\ \hline
\texttt{NON\_EXCLUSIVE}     & 9\,410 & $91.6\%$ \\
\texttt{SUBJECT\_EXCLUSIVE} &   808  & $7.9\%$ \\
\texttt{OBJECT\_EXCLUSIVE}  &    49  & $0.5\%$ \\
\texttt{BOTH\_EXCLUSIVE}    &     9  & $0.1\%$ \\ \hline
\end{tabular}
\caption{Distribución de cardinalidades efectivamente aplicadas por el clasificador sobre las aristas del corpus.}
\label{tab:eval-ragtest-etcdr-cardinality}
\end{table}

Las cifras admiten una lectura combinada con el reparto de cardinalidades. De las 866~aristas con alguna etiqueta exclusiva ($\texttt{SUBJECT}$, $\texttt{OBJECT}$ o $\texttt{BOTH}$), 978 dispararon al menos una consulta de detección con resultado positivo y pasaron por el \emph{Decision Router}\footnote{El número de aristas que activan la detección supera al de aristas con cardinalidad exclusiva porque una arista \texttt{NON\_EXCLUSIVE} también puede entrar a corroboración si encuentra una copia idéntica en el lote.}. La gran mayoría de esas 978~detecciones se resuelve como una de las tres estrategias contextuales: $284$ \texttt{CORROBORATION}, $18$ \texttt{EVOLUTION}, $4$ \texttt{CORRECTION} y $4$ \texttt{DISAGREEMENT}. Las restantes $668$ se resuelven como \texttt{NEW\_EDGE} porque, aunque el detector emitió señal, las descripciones del candidato y de la arista en conflicto convencieron al \emph{Decision Router} de que se trataba de hechos independientes (por ejemplo, dos cargos simultáneos en organizaciones distintas, o dos participaciones en eventos del mismo nombre pero distinto año). Las cuatro únicas decisiones de \texttt{DISAGREEMENT} terminan en aristas con estatus \texttt{disputed}, en consonancia con la política de degradación segura cuando la confianza del LLM cae por debajo de $0.7$.

\begin{table}[h]
\centering
\footnotesize
\begin{tabular}{p{1.7cm}p{6.0cm}p{6.0cm}}
\hline
\textbf{Estrategia} & \textbf{Candidato} & \textbf{Arista en conflicto} \\ \hline
\texttt{EVOLUTION} & \texttt{HELEN CLARK -[EMPLOYED\_AT]-> UNDP} \newline (2017--present) & misma tripleta (2009--2017) \\
\texttt{EVOLUTION} & \texttt{DAVID L. GUNN -[IS\_PRESIDENT\_OF]-> AMTRAK} \newline (2002-05--present) & misma tripleta cerrada en 2002-05 con descripción del cese de 2005 \\
\texttt{CORROBORATION} & \texttt{NIGEL GREENWOOD -[EMPLOYED\_AT]-> HALIFAX TOWN} \newline (1992-07--present) & misma tripleta y mismo período, distinta redacción \\
\texttt{CORRECTION} & \texttt{KLAUS FUCHS -[STUDIED\_AT]-> UNIV.\ LEIPZIG} \newline (1930, según el candidato) & misma tripleta con \texttt{valid\_start} en 1949, claramente errónea \\
\texttt{DISAGREEMENT} & \texttt{CHRIS WATSON -[BORN\_IN]-> VALPARAÍSO} \newline (1867-04, según el candidato) & misma tripleta con período abierto y descripción que admite la duda \\ \hline
\end{tabular}
\caption{Ejemplos representativos, uno por estrategia distinta de \texttt{NEW\_EDGE}, extraídos directamente del \emph{log} de ETCDR sobre el corpus.}
\label{tab:eval-ragtest-etcdr-samples}
\end{table}

La Tabla~\ref{tab:eval-ragtest-etcdr-samples} ilustra el tipo de conflicto que cada una de las cuatro estrategias contextuales captura. La traza de \texttt{HELEN CLARK} es un caso de libro de \texttt{EVOLUTION}: dos documentos describen al mismo cargo en dos intervalos consecutivos y sin solapamiento (2009--2017 y 2017--presente), de modo que el módulo cierra el primero asignándole \texttt{valid\_end~=~2017} e inserta el segundo como sucesor activo. Las cuatro \texttt{CORRECTION} ---entre ellas el caso \texttt{KLAUS FUCHS}--- corresponden a aristas previas cuyo \texttt{valid\_start} es demostrablemente incompatible con la nueva evidencia: el módulo las retracta vía \texttt{tx\_end} sin borrarlas y registra la versión corregida como activa, dejando rastro auditable. Las cuatro \texttt{DISAGREEMENT} aparecen exclusivamente sobre relaciones de lugar de nacimiento o de afiliación con descripciones ambiguas, justamente la situación que el \emph{Decision Router} debe degradar antes que comprometerse con una versión. El \emph{log} no contiene ninguna \emph{late arrival} sobre las 10\,276~aristas, lo que sugiere que el extractor procesa los documentos en un orden lo bastante coherente como para que la cuestión no se materialice en este corpus.

Esta traza confirma, a una escala dos órdenes de magnitud mayor que la del experimento controlado, los dos patrones que aquél anticipaba. Primero, las cinco estrategias del \emph{Decision Router} se distribuyen de forma marcadamente desigual: el caso esperable en un corpus enciclopédico es \texttt{NEW\_EDGE}, las corroboraciones representan un dígito porcentual, y las decisiones de evolución, corrección y desacuerdo se cuentan por decenas. Segundo, las dos estrategias menos frecuentes ---\texttt{CORRECTION} y \texttt{DISAGREEMENT}--- sí aparecen cuando el corpus es lo bastante grande para contenerlas, aunque en proporciones por debajo del $0.1\%$. Su presencia, aun marginal, permite comprobar que la \emph{lógica} de retractación y de degradación de confianza está operativa, una validación que el corpus controlado de la sección~\ref{sec:eval-etcdr} no llega a proporcionar.

%% ============================================================
\section{Evaluación end-to-end sobre TimeQA}\label{sec:eval-timeqa}

Los tres bloques empíricos anteriores han examinado en aislamiento las dos contribuciones algorítmicas de BT-GRAPHRAG (CGER y ETCDR) sobre verdad anotada manualmente y han caracterizado su comportamiento observacional a mayor escala. Esta sección los complementa con una evaluación \emph{end-to-end} sobre un \emph{benchmark} público de preguntas-respuestas temporales: contrasta el motor de consulta temporal de BT-GRAPHRAG contra la línea base de GraphRAG sobre exactamente el mismo corpus y el mismo modelo, de modo que la única variable entre los dos sistemas sea la lógica de razonamiento temporal a la hora de responder. El objetivo es medir qué fracción de la mejora algorítmica del \emph{pipeline} se traduce, efectivamente, en respuestas correctas a preguntas con anclaje temporal explícito.

\subsection{Banco de pruebas y configuración experimental}\label{sec:eval-timeqa-setup}

\paragraph{Conjunto de datos.} La evaluación utiliza el partido \texttt{hard} de TimeQA~\cite{TODO_timeqa}, un \emph{benchmark} de preguntas-respuestas extraído de Wikidata y de Wikipedia, en el que cada pregunta fija una franja temporal explícita (un mes-año, un año, un rango entre dos fechas, o un \emph{before/after} respecto a una fecha) y exige nombrar la entidad que satisface la relación en ese intervalo. El partido \texttt{hard} es el más exigente de TimeQA porque la respuesta requiere atender al modificador temporal: el mismo predicado (por ejemplo, \texttt{employer}) admite distintas respuestas en distintas franjas temporales sobre la misma entidad-sujeto. La evaluación procesa 370~preguntas distribuidas sobre 100~biografías distintas de Wikipedia, cargadas en un único corpus unificado en el que el sistema bajo prueba ha de localizar por sí mismo el documento relevante (no se le entrega el \emph{gold passage}).

\paragraph{Sistemas comparados.} Sobre ese único corpus se comparan tres sistemas:

\begin{itemize}
\item \textbf{GRAPHRAG} (línea base de la literatura): el indexador estándar de GraphRAG~\cite{TODO_graphrag} construye comunidades y \emph{embeddings} sobre el corpus, y el motor de \emph{local search} responde cada pregunta sin ningún componente temporal explícito.
\item \textbf{BT-GRAPHRAG} (\emph{baseline} interno): la misma versión de \emph{local search} de GraphRAG, pero ejecutada sobre el índice (\emph{parquets} + grafo Neo4j) que produce el \emph{pipeline} de indexación bitemporal descrito en el Capítulo~\ref{chapter:proposal} (es decir, con CGER y ETCDR activados en la fase de indexación, pero \emph{sin} usar el motor de consulta temporal). Este sistema aísla el efecto que tiene sobre la línea base la sustitución del indexador estándar por el bitemporal.
\item \textbf{BT-GRAPHRAG} (\emph{temporal query}): el motor de consulta temporal completo, descrito en la sección~\ref{sec:bt-temporal-query}, que extrae sub-consultas con anclaje temporal, resuelve las entidades semilla contra el grafo bitemporal y filtra las aristas candidatas por el período activo. En esta configuración el motor temporal compone su respuesta apoyándose, además, en la respuesta de \emph{local search} de GraphRAG, que se le suministra como \emph{baseline evidence} en el \emph{prompt} de síntesis~\cite{TODO_btg_compose}. La composición es la configuración por defecto del módulo y la que la evaluación reporta.
\end{itemize}

El interés principal de esta sección está en el contraste entre el primero y el tercero (\emph{vanilla} GraphRAG frente al motor temporal completo de BT-GRAPHRAG): los dos sistemas operan sobre el mismo conjunto de documentos, con el mismo modelo de generación y con el mismo \emph{benchmark} de preguntas, de modo que la diferencia entre ellos atribuible a la lógica de razonamiento temporal puede leerse sin confusión. La línea \emph{baseline} interna se reporta como referencia para descontar cuánta variación introduce la sustitución del indexador.

\paragraph{Modelos y parámetros.} Los tres sistemas comparten el mismo modelo de generación, \texttt{gpt-4.1-mini}~\cite{TODO_gpt41mini}, y el mismo modelo de \emph{embedding}, \texttt{text-embedding-3-large}~\cite{TODO_te3large}. El motor temporal de BT-GRAPHRAG opera con la configuración por defecto del Capítulo~\ref{chapter:proposal} ($\tau_{\cos}=0.687$, umbral de confianza del \emph{Decision Router} en $0.7$, máximo de 50~aristas por sub-consulta) y la fase de indexación es exactamente la misma sobre la que se construye el análisis observacional de la sección~\ref{sec:eval-ragtest}. La concurrencia de la evaluación se fija en 100 peticiones simultáneas a la API.

\paragraph{Métrica principal.} Cada predicción se evalúa con un juez LLM (\emph{LLM-as-a-judge})~\cite{TODO_llm_judge} alimentado por el mismo modelo \texttt{gpt-4.1-mini}, instruido con un \emph{prompt} que compara la respuesta candidata con la respuesta de referencia y emite un veredicto binario \texttt{correct} / \texttt{incorrect}. El \emph{prompt} acepta como correctas las paráfrasis, los alias, las normalizaciones tipográficas y las respuestas que añaden contexto no contradictorio, y declara incorrectas las que nombran un referente distinto, las que niegan la referencia o las que se abstienen sin comprometerse con la respuesta. La métrica reportada es la \emph{accuracy} agregada del juez sobre las 370~preguntas. Como métricas complementarias se reportan EM y $F_1$ a nivel de \emph{token} (descontadas en TimeQA por su sensibilidad al fraseo) y métricas operacionales: latencia, número de llamadas al LLM y \emph{tokens} consumidos.

\paragraph{Estabilidad.} Para acotar la varianza inducida por el muestreo del LLM, cada sistema se ejecuta tres veces sobre el mismo conjunto de 370~preguntas, manteniendo fijos los \emph{embeddings} pre-calculados y el grafo persistido. Las métricas agregadas se reportan como media~$\pm$~desviación estándar muestral sobre las tres corridas; los análisis cualitativos por pregunta usan la consolidación por mayoría ($\geq 2$ corridas correctas).

\subsection{Resultados agregados}\label{sec:eval-timeqa-aggregate}

La Tabla~\ref{tab:eval-timeqa-headline} recoge la \emph{accuracy} del juez, el tiempo de pared por corrida y el coste en \emph{tokens} para los tres sistemas. La Tabla~\ref{tab:eval-timeqa-calls} desglosa el reparto de llamadas al LLM entre el modelo de generación y el modelo de \emph{embedding}.

\begin{table}[h]
\centering
\small
\begin{tabular}{lcccc}
\hline
\textbf{Sistema} & \textbf{Accuracy del juez} & \textbf{Tiempo / corrida (s)} & \textbf{\emph{Tokens} / corrida} & \textbf{Latencia / pregunta (s)} \\ \hline
GRAPHRAG (\emph{vanilla})       & $0.687 \pm 0.010$ & $286 \pm 38$ & $3{,}42\,\text{M}$ & $65$ \\
BT-GRAPHRAG (\emph{baseline})   & $0.704 \pm 0.009$ & $663 \pm 2$  & $3{,}46\,\text{M}$ & $75$ \\
BT-GRAPHRAG (\emph{temporal})   & $\mathbf{0.721 \pm 0.011}$ & $658 \pm 6$  & $4{,}07\,\text{M}$ & $83$ \\ \hline
\end{tabular}
\caption{Resultados agregados sobre TimeQA \texttt{hard} (370~preguntas, 3~corridas por sistema). La \emph{accuracy} es la del juez LLM. La latencia por pregunta es la media de los tiempos individuales; el tiempo por corrida es el reloj de pared total con concurrencia $100$.}
\label{tab:eval-timeqa-headline}
\end{table}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrr}
\hline
\textbf{Sistema} & \textbf{Llamadas / corrida} & \textbf{\emph{Tokens prompt}} & \textbf{\emph{Tokens completion}} \\ \hline
GRAPHRAG (\emph{vanilla})       & $1{,}110$ & $3{,}388{,}729$ & $29{,}766$ \\
BT-GRAPHRAG (\emph{baseline})   & $1{,}110$ & $3{,}425{,}625$ & $31{,}072$ \\
BT-GRAPHRAG (\emph{temporal})   & $1{,}872$ & $4{,}008{,}557$ & $65{,}412$ \\ \hline
\end{tabular}
\caption{Reparto del consumo de LLM por sistema sobre TimeQA \texttt{hard} (medias sobre 3~corridas). El motor temporal duplica el número de llamadas porque añade el analizador de sub-consultas y la composición con \emph{baseline evidence}.}
\label{tab:eval-timeqa-calls}
\end{table}

Las cifras permiten una primera lectura. El motor temporal de BT-GRAPHRAG mejora la \emph{accuracy} del juez en $+3.4$~puntos porcentuales sobre la línea base \emph{vanilla} ($0.721$ frente a $0.687$) y en $+1.7$~puntos porcentuales sobre la línea base interna ($0.721$ frente a $0.704$). La diferencia entre las dos líneas base ---ambas usan el mismo motor de \emph{local search} de GraphRAG, pero la segunda opera sobre el índice bitemporal--- queda en $+1.7$~puntos porcentuales, lo que sugiere que aproximadamente la mitad del beneficio que se observa en el sistema completo proviene del propio indexador bitemporal (CGER reduce la fragmentación de entidades y ETCDR consolida aristas redundantes, lo que produce un grafo más limpio para cualquier motor de consulta) y la otra mitad, del motor temporal en sí.

El coste asociado se concentra en dos partidas. El motor temporal consume un $19\%$ más de \emph{tokens} por corrida que la línea base \emph{vanilla} y prácticamente duplica el número de llamadas al LLM, porque añade dos etapas: el analizador de sub-consultas que extrae las entidades semilla y la franja temporal, y la composición con la respuesta de \emph{local search} como \emph{baseline evidence}. El tiempo de pared se multiplica por aproximadamente $2.3\times$ debido a las consultas Cypher contra Neo4j y a la serialización de las sub-consultas; el sobrecosto por pregunta individual ($+18$~s en mediana) es asumible para un sistema interactivo y la curva escala linealmente con la concurrencia. Las desviaciones estándar de la \emph{accuracy} sobre tres corridas son inferiores a $1.1$~puntos porcentuales en los tres sistemas, lo que confirma que el muestreo del LLM no introduce ruido relevante a este orden de magnitud.

\subsection{Divergencia por pregunta}\label{sec:eval-timeqa-divergence}

La métrica agregada esconde el patrón más relevante: \emph{qué preguntas} responde bien cada sistema. La Tabla~\ref{tab:eval-timeqa-divergence} cruza el veredicto del juez para los dos sistemas principales en base de mayoría ($\geq 2$ corridas correctas) y en base estricta ($3/3$ correctas).

\begin{table}[h]
\centering
\small
\begin{tabular}{lrr}
\hline
\textbf{Cruce} & \textbf{Mayoría ($\geq 2/3$)} & \textbf{Estricta ($3/3$)} \\ \hline
Ambas correctas              & $237$ & $210$ \\
Sólo BT-GRAPHRAG correcta    & $\mathbf{30}$ & $\mathbf{17}$ \\
Sólo GRAPHRAG correcta       & $20$ & $6$ \\
Ambas incorrectas            & $83$  & $65$ \\ \hline
\end{tabular}
\caption{Cruce de respuestas correctas entre BT-GRAPHRAG (temporal) y GRAPHRAG (\emph{vanilla}) sobre las 370~preguntas. La columna de mayoría exige acierto en al menos 2 de las 3 corridas; la columna estricta exige acuerdo unánime de las 3 corridas.}
\label{tab:eval-timeqa-divergence}
\end{table}

En base de mayoría, BT-GRAPHRAG acierta 30~preguntas que GRAPHRAG falla; el contraflujo (\emph{vanilla} acierta y BT falla) es de 20~preguntas. Es decir, la mejora neta de $+10$ preguntas en mayoría se compone de $+30$~recuperaciones y $-20$~regresiones. La separación se acentúa al exigir consistencia entre corridas: bajo el criterio estricto ($3/3$~acuerdos), BT-GRAPHRAG retiene $17$~victorias exclusivas frente a sólo $6$~de la línea base, un \emph{ratio} de $2.8\times$. El test de McNemar sobre el cruce de mayoría arroja $p=0.20$ a dos colas, lo que no permite rechazar la hipótesis nula al nivel del $5\%$ sobre las 370~preguntas; el efecto observado es del orden de la magnitud que la potencia del banco permite distinguir, lo que sugiere que para una afirmación estadística más fuerte sobre el cruce agregado sería conveniente ampliar el conjunto de prueba. Lo más informativo, sin embargo, es el desglose por categoría de pregunta que sigue, donde el efecto se concentra en las franjas que la teoría predice.

\subsection{Desglose por tipo de pregunta}\label{sec:eval-timeqa-by-type}

\paragraph{Por tipo de sub-consulta del analizador.} El analizador de sub-consultas del motor temporal etiqueta cada pregunta como \texttt{POINT\_IN\_TIME} (una fecha puntual, por ejemplo ``en Mar 1992'') o como \texttt{RANGE} (un intervalo, por ejemplo ``entre Abr 1996 y Oct 1996'' o un \emph{before/after}). La Tabla~\ref{tab:eval-timeqa-by-querytype} muestra la \emph{accuracy} de cada sistema desagregada por esta etiqueta.

\begin{table}[h]
\centering
\small
\begin{tabular}{lccc}
\hline
\textbf{Tipo de sub-consulta} & \textbf{$n$} & \textbf{GRAPHRAG} & \textbf{BT-GRAPHRAG (temporal)} \\ \hline
\texttt{RANGE}          & $206$ & $0.694$ & $\mathbf{0.748}$ \quad ($+5.4$~pp) \\
\texttt{POINT\_IN\_TIME} & $164$ & $0.695$ & $0.689$ \quad ($-0.6$~pp) \\ \hline
\end{tabular}
\caption{\emph{Accuracy} (base de mayoría) por tipo de sub-consulta extraído por el analizador del motor temporal de BT-GRAPHRAG. La ganancia del motor temporal se concentra en las preguntas de rango, no en las de fecha puntual.}
\label{tab:eval-timeqa-by-querytype}
\end{table}

El efecto del motor temporal está, prácticamente en su totalidad, en las preguntas de rango: las 206 preguntas \texttt{RANGE} concentran $+5.4$~puntos porcentuales de ganancia, mientras que las 164 preguntas \texttt{POINT\_IN\_TIME} quedan en un empate técnico ($-0.6$~puntos porcentuales). La asimetría es coherente con el diseño del módulo: una pregunta puntual admite ser respondida por una sola arista (la que está activa exactamente en esa fecha), de modo que el indexador estándar de GraphRAG, que recupera por similitud semántica, suele encontrar el pasaje correcto sin necesidad de razonamiento temporal explícito. Una pregunta de rango, en cambio, exige razonar sobre intervalos: o bien recuperar varias aristas y elegir la que solapa el período, o bien encontrar una arista cuya descripción cubra todo el intervalo. La indexación bitemporal de ETCDR colapsa varias menciones del mismo hecho en una arista única con período válido $[t_v^s, t_v^e]$, y la consulta temporal filtra por solapamiento de intervalos en Cypher, lo que es exactamente la operación que las preguntas de rango requieren.

\paragraph{Por patrón textual de la pregunta.} La Tabla~\ref{tab:eval-timeqa-by-bucket} reagrupa las 370~preguntas según el patrón sintáctico del modificador temporal en la pregunta original.

\begin{table}[h]
\centering
\small
\begin{tabular}{lccc}
\hline
\textbf{Patrón textual} & \textbf{$n$} & \textbf{GRAPHRAG} & \textbf{BT-GRAPHRAG (temporal)} \\ \hline
\texttt{in Mon YYYY}      & $148$ & $0.703$ & $0.689$  \\
\texttt{between/from-to}  & $141$ & $0.652$ & $\mathbf{0.702}$ \quad ($+5.0$~pp) \\
\texttt{after YYYY}       & $26$  & $0.962$ & $0.962$  \\
\texttt{before YYYY}      & $23$  & $0.652$ & $\mathbf{0.739}$ \quad ($+8.7$~pp) \\
\texttt{in YYYY}          & $13$  & $0.615$ & $\mathbf{0.846}$ \quad ($+23.1$~pp) \\
otros                     & $19$  & $0.684$ & $0.684$  \\ \hline
\end{tabular}
\caption{\emph{Accuracy} (base de mayoría) por patrón textual del modificador temporal de la pregunta. El motor temporal mejora en los tres patrones de rango (\emph{between}, \emph{before}, \emph{in YYYY}) y queda al mismo nivel en los de fecha puntual estrecha (\emph{in Mon YYYY}) y en los de cota \emph{after}.}
\label{tab:eval-timeqa-by-bucket}
\end{table}

La lectura por patrón refina la conclusión anterior. Las preguntas \texttt{between/from-to} son el segmento más grande tras las puntuales y concentran la mayor ganancia bruta del motor temporal ($+7$ preguntas adicionales correctas sobre $141$). Las preguntas \texttt{before YYYY} se benefician en proporción aún mayor ($+2$ sobre $23$, equivalente a $+8.7$~puntos porcentuales). Las preguntas \texttt{in YYYY} ---un año sin mes, una franja de doce meses--- alcanzan la mejora porcentual más alta ($+23$~puntos porcentuales), si bien sobre un soporte reducido de $13$~preguntas. Los dos patrones en los que el motor temporal no aporta (\texttt{in Mon YYYY} y \texttt{after YYYY}) son consistentes con la lectura anterior: el primero es una franja temporal estrechísima (un mes), efectivamente equivalente a un punto, y el segundo admite cualquier sucesor cronológicamente posterior y suele ser fácil de responder por similitud semántica sobre la frase ``after''.

\paragraph{Por tipo de relación (propiedad Wikidata).} Cada pregunta de TimeQA está construida sobre una propiedad Wikidata fija; la Tabla~\ref{tab:eval-timeqa-by-property} reporta la \emph{accuracy} desagregada por las propiedades más frecuentes en el banco.

\begin{table}[h]
\centering
\small
\begin{tabular}{lcccc}
\hline
\textbf{Propiedad} & \textbf{Significado} & \textbf{$n$} & \textbf{GRAPHRAG} & \textbf{BT-GRAPHRAG (temporal)} \\ \hline
\texttt{P54}   & \emph{member of sports team}     & $126$ & $0.675$ & $0.667$ \\
\texttt{P108}  & \emph{employer}                  & $58$  & $0.672$ & $\mathbf{0.845}$ \quad ($+17.3$~pp) \\
\texttt{P39}   & \emph{position held}             & $52$  & $0.692$ & $0.712$ \\
\texttt{P1448} & \emph{official name}             & $21$  & $\mathbf{0.952}$ & $0.810$ \quad ($-14.2$~pp) \\
\texttt{P488}  & \emph{chairperson}               & $19$  & $0.632$ & $0.526$ \\
\texttt{P69}   & \emph{educated at}               & $15$  & $0.600$ & $0.600$ \\
\texttt{P26}   & \emph{spouse}                    & $12$  & $0.833$ & $0.750$ \\
\texttt{P463}  & \emph{member of}                 & $9$   & $0.556$ & $\mathbf{0.778}$ \\
\texttt{P937}  & \emph{work location}             & $9$   & $0.556$ & $\mathbf{0.778}$ \\ \hline
\end{tabular}
\caption{\emph{Accuracy} (base de mayoría) por propiedad Wikidata sobre la que se construye la pregunta. La ganancia del motor temporal se concentra en relaciones de empleo y de pertenencia institucional; las pérdidas, en \emph{official name} y en relaciones casi estáticas (cónyuge, presidente).}
\label{tab:eval-timeqa-by-property}
\end{table}

El reparto por propiedad muestra dos patrones claros. En primer lugar, el motor temporal aporta de forma decisiva sobre \texttt{P108}~(\emph{employer}), donde gana $+10$~preguntas absolutas sobre $58$ (un $+17.3\%$), y sobre las relaciones de pertenencia institucional \texttt{P463}~(\emph{member of}) y \texttt{P937}~(\emph{work location}); son justamente las relaciones que típicamente evolucionan a lo largo de la carrera de una persona, con varios valores válidos en intervalos distintos, y donde la fragmentación temporal del registro penaliza al motor estándar. En segundo lugar, el motor temporal pierde sobre \texttt{P1448}~(\emph{official name}), una propiedad para la que la respuesta es estática o cambia con muy poca frecuencia ($17/21$ frente a $20/21$ para \emph{vanilla}): el sobre-razonamiento temporal puede llevar a seleccionar un nombre interino en lugar del nombre vigente, una observación cualitativa que coincide con los ejemplos del siguiente apartado. \texttt{P54}~(\emph{member of sports team}), la propiedad más frecuente del banco, queda en un empate técnico: las preguntas sobre equipos de fútbol están masivamente sobre-representadas en Wikipedia y la similitud semántica del indexador estándar es muy efectiva para esa franja.

\subsection{Análisis cualitativo de la divergencia}\label{sec:eval-timeqa-qualitative}

Los conjuntos disjuntos de la Tabla~\ref{tab:eval-timeqa-divergence} contienen las preguntas que distinguen empíricamente a los dos sistemas. Esta subsección examina los patrones recurrentes en cada uno de los dos sentidos, sin entrar en una enumeración exhaustiva.

\paragraph{Recuperación por filtrado temporal explícito (BT-GRAPHRAG~$>$~GRAPHRAG).} El patrón más claro de la columna ``sólo BT-GRAPHRAG correcta'' es la \emph{recuperación de hechos que GraphRAG declara desconocer}. En estos casos, la respuesta del motor estándar es alguna variante de ``el conjunto de datos no contiene información sobre…'' a pesar de que el documento de origen está presente en el corpus, mientras que el motor temporal recupera la respuesta correcta. Tres ejemplos de la corrida~1 ilustran el patrón:

\begin{itemize}
\item Pregunta: ``\emph{What was the name of the employer Aurelio Lampredi worked for before Mar 1939?}'', referencia: \texttt{Isotta Fraschini}. GraphRAG: ``\emph{The data provided does not include information about the employer of Aurelio Lampredi before March 1939.}'' BT-GRAPHRAG: ``\emph{Before March 1939, Aurelio Lampredi worked for Isotta Fraschini.}'' La biografía menciona el empleo, pero el indexador estándar no recupera el pasaje porque la similitud semántica entre la pregunta filtrada por fecha y el pasaje narrativo del documento es baja.
\item Pregunta: ``\emph{What was the name of the employer Aurelio Lampredi worked for in Feb 1960?}'', referencia: \texttt{Fiat}. GraphRAG vuelve a abstenerse; BT-GRAPHRAG responde \texttt{Fiat} y aporta el período de cargo.
\item Pregunta: ``\emph{Which team did Ivan Turina play for between May 2004 and May 2006?}'', referencia: \texttt{Dinamo Zagreb}. Misma pauta: GraphRAG se abstiene, BT-GRAPHRAG identifica al portero como titular del Dinamo Zagreb en la temporada 2005--06.
\end{itemize}

El mecanismo subyacente es el siguiente. El motor estándar busca pasajes textualmente similares a la pregunta y, cuando el modificador temporal lo aleja de la formulación literal del documento (que rara vez dice ``\emph{Lampredi's employer between April 1942 and May 1943 was}''), no recupera el pasaje y degrada a una abstención conservadora. El motor temporal, en cambio, descompone la pregunta en ``entidad-semilla'' (``Aurelio Lampredi'') más ``franja temporal'' (\texttt{1942-04-01}--\texttt{1943-05-31}) y lanza una consulta Cypher que selecciona aristas de la entidad-semilla cuyo período válido solape el intervalo pedido. Esa consulta no depende del fraseo de la pregunta, sino de la estructura del grafo, y por tanto recupera la respuesta cuando el grafo contiene la arista correcta. Sobre 30~preguntas (base de mayoría), este patrón explica la mayor parte de las victorias exclusivas del motor temporal.

\paragraph{Regresiones por razonamiento temporal espurio (GRAPHRAG~$>$~BT-GRAPHRAG).} Las 20~preguntas en las que el motor estándar gana exclusivamente sobre el temporal admiten una lectura más matizada y se concentran en dos clases:

\begin{itemize}
\item \emph{Selección errónea entre dos entidades simultáneamente plausibles.} Cuando el grafo contiene más de una arista que solapa el período pedido (por ejemplo, un equipo de origen y un equipo de cesión durante una temporada), el motor temporal puede elegir el equivocado. Ejemplo: ``\emph{Mark Bridge played for which team between Aug 2013 and Dec 2013?}'', referencia: \texttt{Western Sydney Wanderers}. BT-GRAPHRAG responde \texttt{Burton Albion} ---el equipo en el que estuvo cedido en julio de 2013, antes de pasar al club australiano en agosto. El motor estándar, que recupera por similitud semántica del pasaje narrativo, elige correctamente el club principal porque el documento dedica más texto al Western Sydney que al cedente.
\item \emph{Sobre-precisión temporal sobre referentes estables.} En preguntas en las que la respuesta evoluciona poco o nada, el filtrado por franja del motor temporal puede preferir una variante de transición sobre la respuesta canónica. Ejemplo: ``\emph{What was the official name of VP-5 in Nov 1940?}'', referencia: \texttt{Patrol Squadron 42}. BT-GRAPHRAG responde \texttt{Patrol Squadron 17}, un nombre que la unidad ostentó en una franja contigua. La línea base, al no atender al modificador temporal, selecciona el nombre dominante en el pasaje, que coincide con la respuesta correcta.
\end{itemize}

Esta segunda clase de errores es la que explica la regresión observada sobre \texttt{P1448}~(\emph{official name}) en la Tabla~\ref{tab:eval-timeqa-by-property}: la propiedad cambia rara vez y el filtrado temporal estricto no ayuda; puede, en cambio, fijar la respuesta sobre un cambio interino. La primera clase ---selección entre varias aristas plausibles--- es la que limita la capacidad del módulo en relaciones como \texttt{P488}~(\emph{chairperson}) o \texttt{P26}~(\emph{spouse}), donde la elección entre dos referentes cercanos en el tiempo requiere un grado de discriminación contextual que el filtrado temporal puro no aporta.

\paragraph{Coste-beneficio del motor temporal.} Las dos clases anteriores permiten formular el balance con precisión: el motor temporal cambia 30~abstenciones de la línea base por 30~respuestas correctas (la mayor parte de sus victorias exclusivas), a cambio de introducir 20~selecciones incorrectas (la mayor parte de sus regresiones exclusivas). La ganancia neta de $+10$ preguntas sobre $370$ es modesta en términos absolutos, pero la composición de la ganancia es revelador del comportamiento del módulo: el motor temporal sustituye un sesgo de \emph{abstención} por un sesgo de \emph{compromiso}. En aplicaciones que valoran la cobertura sobre la abstención (por ejemplo, un asistente conversacional que prefiere arriesgar una respuesta antes que admitir desconocimiento), este \emph{trade-off} es deseable. En aplicaciones que penalizan más una respuesta incorrecta que una abstención, la composición con \emph{baseline evidence} ---ya activada por defecto, sección~\ref{sec:eval-timeqa-setup}--- modera el sesgo de compromiso al inyectar la respuesta de la línea base como evidencia auxiliar.

\subsection{Lecturas que se desprenden}\label{sec:eval-timeqa-readings}

La evaluación \emph{end-to-end} sobre TimeQA \texttt{hard} confirma, sobre un banco público y con 370~preguntas, los patrones que los experimentos controlados sobre verdad anotada anticipaban: el motor temporal aporta cuando la pregunta exige razonamiento sobre intervalos (preguntas de rango, relaciones que evolucionan a lo largo del tiempo) y no aporta cuando la pregunta es efectivamente puntual o cuando la respuesta es estable. La cuantificación es: $+3.4$~puntos porcentuales de \emph{accuracy} agregada sobre la línea base \emph{vanilla} de GraphRAG y $+1.7$~puntos porcentuales sobre la línea base interna; del motor temporal proviene, por tanto, aproximadamente la mitad de la mejora total, y la otra mitad la aporta el indexador bitemporal (CGER y ETCDR) por el simple hecho de producir un grafo más limpio para el motor estándar.

El experimento también pone una cota al efecto: el coste en \emph{tokens} ($+19\%$) y en latencia ($\times 2.3$) es no despreciable, y la mejora en la métrica agregada queda en el rango de ganancia que la potencia del banco no permite declarar estadísticamente significativa con $p<0.05$ sobre 370~preguntas. La distribución del efecto por categoría ---concentrado en preguntas de rango y en relaciones de empleo, ausente en preguntas puntuales y \emph{official name}--- es, sin embargo, la que la teoría predice, lo que refuerza la lectura de que el motor temporal está midiendo lo que pretende medir. Una evaluación con un banco varias veces mayor o con un partido más temporalmente exigente (preguntas con varias franjas, transiciones cercanas o cancelaciones explícitas) reduciría la varianza y permitiría una afirmación más fuerte sobre la dimensión del efecto.

%% ============================================================
\section{Discusión}\label{sec:eval-discussion}

Los dos experimentos miden propiedades distintas del \emph{pipeline}. El primero, sobre CGER, ataca la limitación~5 del estado del arte (resolución de entidades sin temporalidad): el módulo recupera dos terceras partes de las equivalencias del conjunto de prueba sobre un \emph{baseline} de coincidencia exacta que recupera cero. La $F_1$ pareada de $0.60$ se debe interpretar a la luz de la dificultad del corpus, donde los pares positivos están construidos para no compartir cadena exacta y donde varios alias aparecen una única vez (limitando la calidad del \emph{embedding} de descripción). El umbral coseno calibrado sobre el corpus de entrenamiento mantiene una calidad razonable en transferencia al conjunto de prueba ---la $F_1$ cae de $0.73$ a $0.60$---, lo que se atribuye a la diferencia de densidad de menciones entre los dos corpus y al tamaño limitado de la verdad anotada (33 pares positivos).

El segundo experimento, sobre el sistema combinado de cardinalidad y ETCDR, alcanza una precisión del 98\% en la elección de la estrategia de resolución por arista. Esta cifra debe leerse con cuidado: la verdad anotada del corpus no contiene casos de \texttt{CORRECTION} ni de \texttt{DISAGREEMENT}, por lo que el experimento mide la capacidad del módulo para distinguir entre \texttt{NEW\_EDGE}, \texttt{CORROBORATION} y \texttt{EVOLUTION}, que son las tres estrategias más frecuentes en cualquier corpus biográfico. El resultado confirma que, sobre este reparto, la combinación clasificador de cardinalidad + \emph{Decision Router} es muy estable. Las siete transiciones \texttt{EVOLUTION} se recuperan todas, lo que es relevante porque son precisamente el tipo de decisión que un sistema sin clasificación de cardinalidad asimétrica (la limitación~3 del estado del arte) no podría tomar. Las cinco decisiones incorrectas pertenecen, en su totalidad, a la frontera natural entre \texttt{CORROBORATION} y \texttt{EVOLUTION}: una región donde la verdad anotada por un revisor humano admite, en algunos casos, ambas lecturas legítimas, y no a una insensibilidad estructural del módulo a una categoría de conflicto.

\paragraph{Limitaciones de los experimentos.} Tres limitaciones merecen explicitarse. La primera, ya señalada, es el reducido tamaño de la verdad anotada en CGER (33 pares positivos en prueba, 118 en entrenamiento): el corpus es suficiente para estimar el umbral coseno y para diferenciar el módulo de un \emph{baseline} trivial, pero los intervalos de confianza sobre la $F_1$ pareada son anchos. La segunda es la ausencia de \texttt{CORRECTION} y \texttt{DISAGREEMENT} en la verdad anotada de ETCDR: la composición del corpus no permite valorar la calidad del módulo en esos dos cuadrantes del \emph{Decision Router}, que conceptualmente son los más exigentes; el análisis observacional de la sección~\ref{sec:eval-ragtest} mitiga parcialmente esta limitación al exhibir trazas reales de las dos estrategias, pero sin métricas pareadas. La tercera es la sustitución de CGER por un oráculo de canonicalización en el experimento de ETCDR: el experimento aísla con éxito la calidad de la combinación cardinalidad + \emph{Decision Router}, pero deja para trabajo futuro la medición de los efectos en cascada que un CGER no perfecto introduciría sobre ETCDR.

\paragraph{Lecturas combinadas.} A pesar de las limitaciones anteriores, los tres bloques empíricos permiten una lectura conjunta: BT-GRAPHRAG resuelve la inmensa mayoría de los conflictos temporales del corpus de prueba ($98\%$) bajo el supuesto de que su capa previa de resolución de entidades acierta. Cuando esta última se somete a su propio examen, recupera dos tercios de las equivalencias contra una línea base que recupera ninguna. La traza observacional sobre el corpus de cien documentos refuerza ambas lecturas a una escala mayor: el módulo CGER produce el tipo de fusiones que la literatura clásica espera (acrónimos, expansiones, normalización de signos diacríticos, renombrados institucionales) y rechaza fusiones espurias entre entidades semánticamente vecinas que el coseno por sí solo habría aceptado, mientras que ETCDR distribuye sus estrategias en proporciones consistentes con un corpus enciclopédico (\texttt{NEW\_EDGE} dominante, corroboraciones en el dígito porcentual, evolución/corrección/desacuerdo en el dígito por mil) y materializa las dos estrategias \texttt{CORRECTION} y \texttt{DISAGREEMENT} que el experimento controlado no contenía. La asignación de responsabilidad es, por tanto, transparente: el cuello de botella del \emph{pipeline}, sobre este corpus, es CGER, no ETCDR. Esta lectura orienta la siguiente fase de trabajo hacia el refinamiento del componente de resolución de entidades, donde los \emph{embeddings} de descripción son más cortos y la decisión del LLM se enfrenta a información más esparsa.

%% ============================================================
\section{Conclusiones}\label{sec:eval-conclusions}

El presente capítulo ha evaluado experimentalmente los dos módulos centrales de BT-GRAPHRAG sobre un corpus controlado de cinco biografías. CGER alcanza una $F_1$ pareada de $0.603$ frente a un \emph{baseline} de coincidencia exacta que obtiene cero, lo que confirma la contribución neta del componente sobre la limitación~5 del estado del arte. La combinación de la clasificación de cardinalidad asimétrica y del \emph{Decision Router} de ETCDR alcanza una precisión del 98\% en la asignación de estrategias de resolución, recuperando la totalidad de las transiciones \texttt{EVOLUTION} presentes en la verdad anotada y degradando a \texttt{DISAGREEMENT} sólo aquellos casos en los que la confianza del LLM cae por debajo del umbral configurado.

Los dos experimentos controlados validan empíricamente las dos decisiones de diseño que el Capítulo~\ref{chapter:proposal} consideró más sensibles: el uso del LLM como árbitro contextual de la fusión de entidades, con período activo como parte de su contexto, y la dirección de la búsqueda de conflictos por la cardinalidad asimétrica del tipo de relación. El análisis observacional sobre el corpus de cien documentos confirma a una escala mayor las distribuciones que aquellos experimentos anticipaban y, al exhibir trazas reales de las estrategias \texttt{CORRECTION} y \texttt{DISAGREEMENT}, comprueba que la lógica de retractación y de degradación de confianza está operativa, aunque su evaluación cuantitativa sobre verdad anotada queda pendiente para corpus con retractaciones explícitas. El análisis sugiere, además, que el cuello de botella sobre estos corpus es la fase de resolución de entidades y no la de detección y resolución de conflictos, lo que orienta el trabajo futuro hacia el refinamiento de CGER en escenarios de descripción esparsa.
